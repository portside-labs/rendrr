## What this changes

<!-- A sentence or two. Link the issue if there is one. -->

## Why

<!-- What problem this solves. For behaviour changes, say what was wrong before. -->

## Checklist

- [ ] `cargo fmt` is clean
- [ ] `cargo clippy --all-targets -- -D warnings` passes
- [ ] `cargo test` passes
- [ ] Tests cover the change (a bug fix should include a test that fails without it)
- [ ] Docs updated if behaviour or configuration changed (`README.md`, `.env.example`, `docs/`, `docs/public/openapi.yaml`)

## Notes for the reviewer

<!-- Anything non-obvious: a tradeoff you made, a case you deliberately left out. -->
